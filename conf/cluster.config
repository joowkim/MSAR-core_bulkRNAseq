singularity {
	enabled = true
	autoMounts = true
}

executor {
	name='slurm'
    queueSize = 12 // # of tasks the executor will handle in a parallel manner
    clusterOptions = "--partition=defq "
    // cpus = 4
    // memory = 8.GB
    // time = '12h'
}