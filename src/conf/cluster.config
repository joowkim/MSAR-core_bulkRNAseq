singularity {
	enabled = true
	autoMounts = true
}

executor {
	name='slurm'
    queueSize = 20 // # of tasks the executor will handle in a parallel manner
    // clusterOptions = '--ntasks-per-node=1 '
    // cpus = 4
    // memory = 8.GB
    // time = '12h'
}