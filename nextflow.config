// process.executor='slurm'

//executor {
    // name='slurm'
    // queueSize = 8 // # of tasks the executor will handle in a parallel manner
    // clusterOptions = '--ntasks-per-node=1 --export=NONE --export=NTHREADS --mem-bind=local'
    // cpus = 4
    // memory = 8.GB
    // time = '12h'
//}